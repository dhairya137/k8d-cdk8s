# https://github.com/adyanth/cloudflare-operator

apiVersion: networking.cfargotunnel.com/v1alpha1
kind: ClusterTunnel
metadata:
  name: k3s-cluster-tunnel      # The ClusterTunnel Custom Resource Name
spec:
  newTunnel:
    name: my-k8s-tunnel         # Name of your new tunnel on Cloudflare
  size: 2                       # This is the number of replicas for cloudflared
  cloudflare:
    email: valorantbadboy@gmail.com    # Your email used to login to the Cloudflare Dashboard
    domain: doapps.pro         # Domain under which the tunnel runs and adds DNS entries to
    secret: cloudflare-secrets  # The secret created before
    accountId: e3d30d0e27faf0a26598708ad004b977